// Umbrella header for the vendored zstd decoder.
//
// `zstddeclib.c` is the amalgamated single-file decompressor produced by zstd's own
// `build/single_file_libs/combine.py` (see Sources/CZstd/README.md for the exact command and version).
// It needs no separate translation units and no build configuration.
#include "zstd.h"
